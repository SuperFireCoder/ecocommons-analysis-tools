export interface Workflow {
    id: string;
    title: string;
    description: string;
    url: string;
    imagePath: string;
}

export interface Page {
    key: string;
    href: string;
    label: string;
}